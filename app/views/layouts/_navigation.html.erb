<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Navigation</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item <% if params[:action] == "index" %>active<% end %>">
        <%= link_to "List All Posts", posts_path, class: "nav-link" %>
      </li>
      <li class="nav-item<% if params[:action] == "published" %> active<% end %>">
        <%= link_to "List Published Posts", published_posts_path, class: "nav-link" %>
      </li>
      <li class="nav-item<% if params[:action] == "new" %> active<% end %>">
        <%= link_to "Create New", new_post_path, class: "nav-link" %>
      </li>
    </ul>
  </div>
</nav>